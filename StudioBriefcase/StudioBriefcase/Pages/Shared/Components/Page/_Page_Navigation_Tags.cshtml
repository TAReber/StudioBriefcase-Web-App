@* NOTE:
    This partial is Dynamically loaded and used in mutliple areas.
    SubmissionForms with 0 are the navigator.

    DEVELOPMENT TASK
        -The Tuple used in the partial Async can Defaults to 0, but can be used to load user saved values.
*@

@model StudioBriefcase.Models.TagsModel

<div class="box-row box-center2">
    <div class="box-col">
        <label for="PostType">OS:</label>
        <select name="OS" id="PageTag-OS">
            <option value="0">Any OS</option>
            @await Html.PartialAsync("/Pages/Shared/Components/Base/_SelectOptions.cshtml", Tuple.Create(Model.TagLists.Tags_OS, Model.ids.OS))
        </select>
    </div>

    <div class="box-col">
        <label for="PostType">IDE</label>
        <select name="OS" id="PageTag-IDE">
            <option value="0">Any IDE</option>
            @await Html.PartialAsync("/Pages/Shared/Components/Base/_SelectOptions.cshtml", Tuple.Create(Model.TagLists.Tags_IDE, Model.ids.IDE))
        </select>
    </div>
</div>

<div class="box-row box-center2">
    <div class="box-col" style="width:30%;">
        <label for="Tag1">Tag 1</label>
        <select name="Tag1" id="PageTag-1">
            <option value="0">Any</option>
            @await Html.PartialAsync("/Pages/Shared/Components/Base/_SelectOptions.cshtml", Tuple.Create(Model.TagLists.Tags_normal, Model.ids.Tag1)) 
        </select>
    </div>
    <div class="box-col" style="width:30%;">
        <label for="Tag2">Tag 2</label>
        <select name="Tag1" id="PageTag-2">
            <option value="0">Any</option>
            @await Html.PartialAsync("/Pages/Shared/Components/Base/_SelectOptions.cshtml", Tuple.Create(Model.TagLists.Tags_normal, Model.ids.Tag2))
        </select>
    </div>
    <div class="box-col" style="width:30%;">
        <label for="Tag1">Tag 3</label>
        <select name="Tag3" id="PageTag-3">
            <option value="0">Any</option>
            @await Html.PartialAsync("/Pages/Shared/Components/Base/_SelectOptions.cshtml", Tuple.Create(Model.TagLists.Tags_normal, Model.ids.Tag3))
        </select>
    </div>
</div>